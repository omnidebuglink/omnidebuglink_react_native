<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <!-- get_state reads ConnectivityManager; release manifests of host apps
       usually only carry INTERNET, so the SDK declares it itself (merges in) -->
  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
</manifest>
